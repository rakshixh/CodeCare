module.exports = {
  defaultSizeLimit: 50000, // Default size limit for large files in bytes
  defaultDirectory: "./reports", // Default directory to save reports
  defaultOutput: "html", // Default report format (html/json)
  defaultMaxLines: 500, // Default maximum lines of code allowed in a single file
};
